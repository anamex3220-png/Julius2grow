{
  "name": "julius2grow",
  "private": true,
  "version": "1.0.0",
  "description": "Reclutamiento anti-curriculum: retos de 15 minutos en vez de hojas de vida.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "install:all": "npm install --workspace server && npm install --workspace client",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
