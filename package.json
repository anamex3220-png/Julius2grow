{
  "name": "julius2grow",
  "private": true,
  "version": "1.0.0",
  "description": "Anti-AI-Retos: retos rápidos y técnicos para evaluar talento de forma más eficaz que un CV.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "install:all": "npm install --workspace server && npm install --workspace client",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
